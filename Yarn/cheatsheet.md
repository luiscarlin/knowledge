## Cheatsheet

```bash
# install yarn
npm install yarn --global

# save as dependency
yarn add package

# save as dev dependency
yarn add package --dev

# remove dependency
yarn remove package

# check why is package needed
yarn why package
```
